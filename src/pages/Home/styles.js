import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles(() => ({
    centralBox: {
        display: 'flex',
        justifyContent: 'center',
        marginTop: '20px',
    },
    fadeCard: {
        position: 'absolute',
    },
}));
